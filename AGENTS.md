# ChatApp Realtime agent 约定

改代码前先读相关实现、调用方、迁移和测试，确认消息、事务、租约、幂等与重放语义；不要为表面相似的代码提前抽象。

优先级是正确与安全、可维护、可测量的性能、真实复用。只共享稳定、线程安全且生命周期匹配的连接池、不可变配置、serializer 或有界调度器；禁止共享 `DbContext`、command/reader、事务、流和可变会话，池化对象必须清零并可靠归还。

性能改动必须有基线，优先减少数据库/消息队列往返、WAL 和热路径分配；复用至少需要两个真实调用方和清晰所有权。验证按聚焦单测/契约测试 → Release 构建 → 短时 admission/smoke 推进，阶段长测和发布 soak 留到功能冻结后。

下一阶段与接手状态见 `docs/NEXT-STAGE.md`。
